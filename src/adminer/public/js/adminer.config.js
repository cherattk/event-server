const app_url = 'http://localhost:3000/adminer';
const eventmap_url = `${app_url}/event-map`;
const activity_url = `${app_url}/activity`;

export default {
  app_url : app_url,
  eventmap_url : eventmap_url,
  activity_url : activity_url
}